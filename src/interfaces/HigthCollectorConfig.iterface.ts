export interface HigthCollectorConfig {
    apiKey: string;
    apiUrl: string;
    step: number;
    queryLimit: number;
    queryDelayMs: number;
    startPoint: any; // ??
    endPoint: any; // ??
}